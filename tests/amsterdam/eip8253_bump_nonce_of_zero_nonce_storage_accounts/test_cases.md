| Proposed test                                          | Setup                                                                                                 | Critical BAL expectation                                                                                                                                                                                                                |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test_bal_nonce_bump_fork_transition`                  | Empty activation block                                                                                | Every EIP-8253 target appears exactly once with `nonce_changes=[[0,1]]`; no balance/code/storage entries from the bump. Normal pre-exec system entries coexist at index 0.                                                              |
| `test_bal_nonce_bump_full_target_list`                 | Activation block using complete Mainnet target list                                                   | All 28 targets included, none omitted/duplicated, lexicographically sorted.                                                                                                                                                             |
| `test_bal_nonce_bump_preserves_account_state`          | Targets have balance + non-empty storage                                                              | Only nonce changes. Balance, code, storage and storage root remain unchanged; existing storage slots MUST NOT appear as BAL storage reads/changes.                                                                                      |
| `test_bal_nonce_bump_state_root_reconstruction`        | Empty activation block                                                                                | Applying the BAL directly to the pre-fork parent state reproduces the execution-derived post-state root. This is explicitly required by EIP-8253. ([Ethereum Improvement Proposals][1])                                                 |
| `test_bal_nonce_bump_only_at_transition`               | Three blocks: pre-fork, activation, post-fork                                                         | Pre-fork nonce=0; activation has `[0,1]`; following block MUST NOT contain another nonce bump.                                                                                                                                          |
| `test_bal_nonce_bump_post_fork_untouched_absent`       | Block immediately after activation, target untouched                                                  | Target absent from that block's BAL entirely.                                                                                                                                                                                           |
| `test_bal_nonce_bump_post_fork_access_only`            | Following block calls/BALANCEs target                                                                 | Target appears with empty changes; `[0,1]` MUST NOT repeat.                                                                                                                                                                             |
| `test_bal_nonce_bump_and_read_only_access`             | Activation block CALL/BALANCE/EXTCODEHASH against target                                              | Still exactly one account entry and only `nonce_changes=[[0,1]]`. No duplicate "access-only" account entry.                                                                                                                             |
| `test_bal_nonce_bump_and_zero_value_transfer`          | Activation tx sends zero ETH to target                                                                | Same: one account entry, `[0,1]`, no balance change.                                                                                                                                                                                    |
| `test_bal_nonce_bump_and_value_transfer`               | Activation tx sends value to target                                                                   | `nonce_changes=[[0,1]]` plus `balance_changes=[[1, post_balance]]`.                                                                                                                                                                     |
| `test_bal_nonce_bump_and_reverted_value_transfer`      | Contract transfers value to target, then enclosing frame reverts                                      | Fork nonce bump survives; transaction balance change disappears. Target remains with `[0,1]`. This specifically catches journaling accidentally reverting pre-tx BAL state.                                                             |
| `test_bal_nonce_bump_access_oog_before_target`         | CALL/BALANCE/etc. fails before target state access                                                    | Target is nevertheless present because of the fork transition, with `[0,1]`. No transaction-derived fields. This differs from normal BAL tests where such a target is absent.                                                           |
| `test_bal_nonce_bump_2930_listed_but_untouched`        | Target occurs in EIP-2930 access list but isn't accessed                                              | Target still appears because of the nonce bump, not because of EIP-2930. Only `[0,1]`. This is the inverse of existing `test_bal_2930_account_listed_but_untouched`.                                                                    |
| `test_bal_nonce_bump_create_collision`                 | First activation-block tx attempts CREATE/CREATE2 at target                                           | Target has only `[0,1]`. No tx-index nonce/code/storage changes. Creation fails because post-transition nonce is already 1.                                                                                                             |
| `test_bal_nonce_bump_create_collision_no_storage_read` | Same, target has its historical non-empty storage                                                     | `storage_reads=[]`. Collision must occur from `nonce != 0`; clients must not consult/record target storage. This is particularly useful for distinguishing the EIP-8253 path from an EIP-7610-style storage check.                      |
| `test_bal_nonce_bump_create_early_failure`             | CREATE/CREATE2 would target A but fails before target access: OOG/insufficient balance/static-context | A still appears with `[0,1]` because of the fork transition. There must be no tx-derived change for A. Parameterize over the existing CREATE failure boundaries if the fixture allows targeting A.                                      |
| `test_bal_nonce_bump_create_collision_next_block`      | CREATE collision against A one block after activation                                                 | A has no nonce change; it appears only as an accessed collision target with empty changes. Ensures the irregular transition is not replayed.                                                                                            |
| `test_bal_nonce_bump_selfdestruct_beneficiary`         | Positive-balance SELFDESTRUCT to target                                                               | Target has `[0,1]` plus the tx-index balance change. No execution/code effects on target.                                                                                                                                               |
| `test_bal_nonce_bump_zero_balance_selfdestruct`        | Zero-balance SELFDESTRUCT to target                                                                   | Target retains only `[0,1]`; no spurious balance change.                                                                                                                                                                                |
| `test_bal_nonce_bump_reverted_selfdestruct`            | SELFDESTRUCT transfers to target inside reverted frame                                                | Target's index-0 nonce survives; reverted balance transfer MUST NOT appear.                                                                                                                                                             |
| `test_bal_nonce_bump_coinbase_empty_block`             | `coinbase == target`, zero transactions                                                               | Target MUST be present because of `[0,1]`, despite the normal rule that an untouched coinbase is absent. This directly interacts with `test_bal_empty_block_no_coinbase`.                                                               |
| `test_bal_nonce_bump_coinbase_zero_tip`                | `coinbase == target`, tx with zero priority fee                                                       | `[0,1]`, but no balance change.                                                                                                                                                                                                         |
| `test_bal_nonce_bump_coinbase_fees`                    | `coinbase == target`, positive priority fee                                                           | `[0,1]` plus tx-index post-fee balance.                                                                                                                                                                                                 |
| `test_bal_nonce_bump_recipient_and_coinbase_same_tx`   | Same target is transaction recipient and fee recipient                                                | Exactly one `balance_changes` entry at that tx index containing the final combined post-tx balance, plus `[0,1]`. This checks same-index coalescing.                                                                                    |
| `test_bal_nonce_bump_withdrawal`                       | Withdrawal to target                                                                                  | `[0,1]` plus `balance_changes=[[n+1, post_balance]]`.                                                                                                                                                                                   |
| `test_bal_nonce_bump_zero_withdrawal`                  | Zero-amount withdrawal to target                                                                      | Only `[0,1]`; no balance change. Existing zero-withdrawal behavior expects an empty account entry otherwise.                                                                                                                            |
| `test_bal_nonce_bump_multiple_withdrawals`             | Multiple withdrawals to same target                                                                   | One final balance entry at `n+1`, not one per withdrawal, plus `[0,1]`.                                                                                                                                                                 |
| `test_bal_nonce_bump_tx_then_withdrawal`               | Target receives value in tx and withdrawal afterward                                                  | `nonce=[[0,1]]`, balance entries at tx index and `n+1`.                                                                                                                                                                                 |
| `test_bal_nonce_bump_balance_same_index_coalescing`    | Target receives value through multiple mechanisms within one tx, e.g. recipient + coinbase            | One final post-tx balance entry for the index, not multiple entries.                                                                                                                                                                    |
| `test_bal_nonce_bump_size_limit`                       | Empty activation block with gas limit exactly at BAL item boundary                                    | The 28 bumped addresses count as 28 BAL items. Their 129 historical storage slots do **not** count because they aren't accessed. Exact limit succeeds; one below fails. This should extend the existing fork-transition BAL-size test.  |
| `test_bal_nonce_bump_size_limit_address_dedup`         | One bumped target is also coinbase/withdrawal recipient/tx recipient                                  | That address counts once, not twice, toward `bal_items`.                                                                                                                                                                                |
| `test_bal_nonce_bump_index_zero_rlp_encoding`          | Corrupt the RLP encoding of `[0,1]`                                                                   | Specifically test canonical encoding of `block_access_index=0`. Existing non-minimal-scalar coverage uses ordinary transaction indices; zero has its own RLP representation and is worth explicit coverage.                             |
| `test_bal_invalid_missing_bumped_account`              | Remove one untouched target entirely                                                                  | `INVALID_BLOCK_ACCESS_LIST`.                                                                                                                                                                                                            |
| `test_bal_invalid_missing_bump_on_merged_account`      | Target also receives value; leave balance entry but delete `[0,1]`                                    | Invalid. Important because account itself remains present, so this specifically verifies the missing field rather than missing account.                                                                                                 |
| `test_bal_invalid_bump_nonce_value`                    | Replace `[0,1]` with `[0,2]`                                                                          | Invalid.                                                                                                                                                                                                                                |
| `test_bal_invalid_bump_index`                          | Replace `[0,1]` with `[1,1]` or `[n+1,1]`                                                             | Invalid.                                                                                                                                                                                                                                |
| `test_bal_invalid_duplicate_bump_index`                | Two nonce entries at index 0                                                                          | Invalid uniqueness violation.                                                                                                                                                                                                           |
| `test_bal_invalid_duplicate_target_account`            | Builder emits one account entry for fork transition and another for tx activity                       | Invalid. Fork and tx changes MUST merge into the single `AccountChanges`.                                                                                                                                                               |
| `test_bal_invalid_spurious_bump_field`                 | Untouched target gets fake balance/code/storage entry in addition to `[0,1]`                          | Invalid.                                                                                                                                                                                                                                |
| `test_bal_invalid_missing_later_target_change`         | Correct `[0,1]`, but remove a legitimate tx/withdrawal balance change                                 | Invalid. Ensures the fork entry cannot mask missing later state changes.                                                                                                                                                                |
| `test_bal_invalid_target_ordering`                     | Move one transition target out of global lexicographic order                                          | Invalid. Transition entries are not a separate prefix; they participate in normal account sorting.                                                                                                                                      |
| `test_bal_non_target_same_shape`                       | Another zero-nonce, empty-code, non-empty-storage account is not in the EIP-8253 list                 | It stays nonce 0. If untouched, absent from BAL; if accessed, present without nonce change. Prevents implementations from scanning/mutating by shape instead of using the fixed list.                                                   |

[1]: https://eips.ethereum.org/EIPS/eip-8253 "https://eips.ethereum.org/EIPS/eip-8253"

